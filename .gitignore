# Dependencies
/node_modules

# Production build output (`npm run build` — compiled JS/CSS, not source)
/build

# Tooling / local env
.DS_Store
.env.local
.env.*.local
npm-debug.log*
yarn-debug.log*
yarn-error.log*
